# 通讯协议总结（维特 WIT 私有协议）

> 来源：`通讯协议.pdf`（标题《WIT私有协议》，共 30 页）
> 适用设备：维特 IMU/姿态传感器模块（JY901、WT901、IWT603、HWT906 等系列）
> 本文面向 STM32 端（sea_sensor 项目）实现串口通信与数据解析时使用的速查参考。

---

## 1. 总体说明

- 所有数据均为 **16 进制（HEX）** 字节流，**不是 ASCII**。
- 每个 16 位数据分 **低字节(L) + 高字节(H)** 依次传送，组合成 **有符号 short**：
  `DATA = (short)((short)DATAH << 8 | DATAL)` —— 注意高字节必须先转成有符号 short 再移位，否则负数会出错。
- 输出的数据都带正负，需做符号位处理。
- 串口写指令 **必须在 10 秒内完成**，否则模块自动上锁。写操作必须走「解锁 → 写指令 → 保存」三步。

---

## 2. 帧格式

### 2.1 主动上传帧（模块自动回传，读数据也走此格式）

| 字节 | 内容 |
|------|------|
| 0 | 帧头 `0x55` |
| 1 | `TYPE` 数据类型 |
| 2~3 | DATA1L DATA1H |
| 4~5 | DATA2L DATA2H |
| 6~7 | DATA3L DATA3H |
| 8~9 | DATA4L DATA4H |
| 10 | `SUMCRC` 校验和（低 8 位） |

`SUMCRC = 0x55 + TYPE + 所有数据字节之和`，取结果的低 8 位（char 型）。

### 2.2 写指令帧（主机 → 模块）

| 字节 | 内容 |
|------|------|
| 0 | 协议头 `0xFF` |
| 1 | 协议头 `0xAA` |
| 2 | 寄存器地址 `ADDR` |
| 3 | 数据低 8 位 `DATAL` |
| 4 | 数据高 8 位 `DATAH` |

### 2.3 数据读取（READADDR 机制）

- 写 `READADDR` 寄存器（地址 `0x27`）指定要读的寄存器地址。
- 模块固定返回 **连续 4 个寄存器** 的值：
  `0x55 0x5F REG1L REG1H REG2L REG2H REG3L REG3H REG4L REG4H SUM`。
- 例：读取加速度 X 轴（`0x34`）：发 `FF AA 27 34 00`，回 `55 5F AXL AXH AYL AYH AZL AZH GXL GXH SUM`。

---

## 3. 数据类型（TYPE 字节）

| TYPE | 内容 | 关键解析公式 |
|------|------|--------------|
| `0x50` | 时间 | 年/月/日/时/分/秒 + 毫秒 `((MSH<<8)\|MSL)` |
| `0x51` | 加速度 | `Ax = ((AxH<<8)\|AxL)/32768*16g`（g≈9.8）；温度 `T=((TH<<8)\|TL)/100 ℃` |
| `0x52` | 角速度 | `Wx = /32768*2000 °/s`（WT9011G4K 改用 4000°/s 系数） |
| `0x53` | 角度 | `Roll/Pitch/Yaw = /32768*180 °`；末 2 字节为版本号 |
| `0x54` | 磁场 | `Hx = ((HxH<<8)\|HxL)`（原始 LSB，毫高斯量级） |
| `0x55` | 端口状态 | `DxStatus`；模拟输入时 `U = DxStatus/1024*Uvcc` |
| `0x56` | 气压高度 | 气压 `((P3<<24)\|(P2<<16)\|(P1<<8)\|P0) Pa`；高度 `cm` |
| `0x57` | 经纬度 | `Lon/Lat` 32 位；`dd = val/10000000`，`mm.mmmmm = (val%10000000)/100000`；正=北/东，负=南/西 |
| `0x58` | 地速 | GPS 海拔/10 m、航向/100°、地速/1000 km/h |
| `0x59` | 四元数 | `q0~q3 = /32768` |
| `0x5A` | GPS 精度 | 卫星数、PDOP/HDOP/VDOP（均 /100） |
| `0x5F` | 读取返回 | 见 2.3 |

---

## 4. 指令操作流程（通用三步）

```
1. 解锁:   FF AA 69 88 B5
2. 写指令: FF AA <ADDR> <DATAL> <DATAH>
3. 保存:   FF AA 00 00 00
```
- 读取指令无需解锁。
- 修改波特率后需先切到新波特率，再重新发解锁+保存。

---

## 5. 常用寄存器速查（地址 / 默认 / 说明）

| 地址(Hex) | 名称 | 默认 | 说明 |
|-----------|------|------|------|
| `00` | SAVE | 0x0000 | 0x00 保存 / 0xFF 重启 / 0x01 恢复出厂 |
| `01` | CALSW | 0x0000 | 校准模式：01 加计，03 高度清零，04 航向置零，07 磁场(球)，08 角度参考，09 磁场(双平面) |
| `02` | RSW | 0x001E | 输出内容开关（bit0~10 对应 TIME~GSA） |
| `03` | RRATE | 0x0006 | 输出速率：1=0.2Hz … 6=10Hz … 9=100Hz，C=单次，D=不回传 |
| `04` | BAUD | 0x0002 | 波特率：2=9600，6=115200（默认），8/9 仅部分型号 |
| `05~0D` | AX/AZ/GX/GZ/HX/HZ OFFSET | 0 | 零偏；加计零偏 = val/10000 g，陀螺 = val/10000 °/s |
| `0E~11` | D0~D3MODE | 0 | 端口模式：0 模拟输入 /1 数字输入 /2 输出高 /3 输出低 /5 相对姿态 |
| `1A` | IICADDR | 0x50 | I2C/Modbus 设备地址 0x01~0x7F |
| `1F` | BANDWIDTH | 0x0004 | 带宽 0=256Hz … 4=20Hz … 6=5Hz |
| `20` | GYRORANGE | 0x0003 | 陀螺量程 0=200 … 3=2000°/s |
| `21` | ACCRANGE | 0x0000 | 加速度量程 0=±2g … 3=±16g |
| `22` | SLEEP | 0 | 1=休眠（串口任意数据唤醒） |
| `23` | ORIENT | 0 | 0 水平 / 1 垂直安装 |
| `24` | AXIS6 | 0 | 0 九轴算法（绝对航向）/ 1 六轴算法（相对航向） |
| `25` | FILTK | 0x001E | 动态 K 值滤波，1~10000（越小越信陀螺） |
| `26` | GPSBAUD | 0x0002 | GPS 波特率 |
| `27` | READADDR | 0x00FF | 读取寄存器地址 |
| `2A` | ACCFILT | 0x01F4 | 加速度滤波 1~10000 |
| `2D` | POWONSEND | 0x0001 | 上电输出开关 |
| `2E` | VERSION | — | 只读版本号 |
| `30~33` | YYMM/DDHH/MMSS/MS | 0 | 片上时间 |
| `34~3F` | AX~Yaw | 0 | 只读实时量（加速度/角速度/磁场/角度） |
| `40` | TEMP | 0 | 温度 /100 ℃ |
| `45~48` | PressureL~HeightH | 0 | 气压(Pa)/高度(cm) |
| `49~50` | LonL~GPSVH | 0 | 经纬度、GPS 海拔/航向/地速 |
| `51~54` | q0~q3 | 0 | 四元数 /32768 |
| `55~58` | SVNUM~VDOP | 0 | GPS 卫星数/精度 |
| `59` | DELAYT | 0 | 报警信号延时(ms) |
| `5A~5B` | XMIN/XMAX | 0 | X 轴报警阈值 = val*180/32768 ° |
| `5C` | BATVAL | 0 | 电压 /100 |
| `5D` | ALARMPIN | 0x4365 | 报警引脚映射（X-/X+/Y-/Y+ → D0~D3/SCL/SDA） |
| `5E~5F` | YMIN/YMAX | 0 | Y 轴报警阈值 |
| `61` | GYROCALITHR | 0 | 陀螺静止阈值 = val/1000 °/s（建议 = WERROR*1.2） |
| `62` | ALARMLEVEL | 0 | 0 低电平报警 / 1 高电平报警 |
| `63` | GYROCALTIME | 0x03E8 | 陀螺自动校准静止时间(ms) |
| `68` | TRIGTIME | 0 | 报警连续触发时间(ms) |
| `6A` | WERROR | 0 | 只读陀螺变化值（用于标定 GYROCALITHR） |
| `6B` | TIMEZONE | 0x0014 | GPS 时区（0x14=UTC+8） |
| `6E` | WZTIME | 0x01F4 | 角速度连续静止时间(ms) |
| `6F` | WZSTATIC | 0x012C | 角速度积分阈值 = val/1000 °/s |
| `74` | MODDELAY | 0x0BB8 | 485 应答延时(us，仅 Modbus 版) |
| `79~7A` | XREFROLL/YREFPITCH | 0 | 角度零位参考 = val/32768*180 ° |
| `7F~84` | NUMBERID1~6 | — | 只读设备编号 |
| `94` | GSTATICTHRE | 50 | 静止判断阈值（×1000，单位 °） |
| `95` | GSTATICTIME | 5000 | 静止判断时间(ms) |
| `96` | GSTATICFLAG | 0 | 只读：1 静止 / 0 运动 |

---

## 6. 校准与配置流程示例

- **加速度校准**：解锁 → `FF AA 01 01 00` → 延时 4s → `FF AA 01 00 00` → 延时 100ms → 保存。
- **角度参考（XY 归零）**：解锁 → `FF AA 01 08 00` → 延时 3s → 保存。
- **Z 轴（航向）置零**：解锁 → `FF AA 01 04 00` → 延时 3s → 保存（需六轴算法）。
- **修改波特率**：解锁 → `FF AA 04 06 00`（设 115200）→ 切波特率 → 重新解锁 → 保存。
- **陀螺仪自动校准开关**：`FF AA 61 00 00`（开，默认）/ `01 00`（关）。
- **陀螺仪手动校准**：先关自动校准 → 静止时 `FF AA 01 0C 00` 取零偏 → `FF AA 01 0D 00` 结束 → 保存。
- **磁场校准**：`FF AA 01 07 00` 后绕三轴各转圈 → `FF AA 01 00 00` 退出 → 保存（5 分钟超时）。
- **时间校准**：分别写 `30`(年月)/`31`(日时)/`32`(分秒)/`33`(毫秒) 后保存。

---

## 7. 给 STM32 实现的要点提示

1. 串口按 **HEX 字节** 解析，不要做 ASCII 转换。
2. 解析上行帧：找 `0x55` 帧头 → 取 TYPE → 按表拼 16 位有符号数 → 计算 `SUMCRC` 校验（低 8 位）。
3. 写参数必须先 `FF AA 69 88 B5` 解锁，且整套流程在 10 秒内完成，最后 `FF AA 00 00 00` 保存，否则修改不生效（自动上锁）。
4. 负数处理：所有 16 位量按 `int16_t` 解析（高字节符号扩展）。
5. 型号差异：部分寄存器/速率/波特率仅特定型号支持（WT9011G4K 角速度系数 4000°/s；HWT906/JY931 支持 460800/921600 波特与 500/1000Hz；JY901L 支持报警输出时间与震动唤醒）。
